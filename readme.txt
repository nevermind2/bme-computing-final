Each project statement has its own script.  The 'main.m' script calls all the scripts.

"Image_Segmentation.m' is basically all the scripts in one script, with comments.

"Image Segmentation - Write Up.pdf' is the write up.